
@model TrashCollector2.Models.CustomerDayViewModel

@{
    ViewBag.Title = "PickUps";
}

<h2>PickUps</h2>

<div class="form-horizontal">
    <h4>Daily Pick Ups</h4>
    <hr />
    @Html.ValidationSummary(true, "", new { @class = "text-danger" })

    <div class="form-group">
        @Html.LabelFor(model => model.PickUpDay, "Pick Up Day", new { @class = "control-label col-md-2" })
        <div class="col-md-10">
            @*@Html.DropDownListFor(model => model.PickUpDay, new SelectList(Model.PickUpDay, "Id", "Name"), "", new { @class = "form-control" })*@
            @Html.DropDownList("Pick Up Day", new SelectList(ViewBag.Days), new { @class = "form-control" })
            @Html.ValidationMessage("Pick Up Day", "", new { @class = "text-danger" })
        </div>
    </div>
    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <input type="submit" value="Select" class="btn btn-default" />
        </div>
    </div>
</div>

<table class="table">
    @*<tr>
            <th>
                @Html.DisplayNameFor(model => model.UserName)
            </th>
            <th></th>
        </tr>
        <div class="form-group">
            @Html.LabelFor(model => model, htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.DropDownList("PickUpDay", (SelectList)ViewBag.Days, " ")*@
    @*@Html.ValidationMessageFor(model => model.employeeSelection, "", new { @class = "text-danger" })*@
    @*</div>
        </div>*@
    @foreach (var item in Model.Customers)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.LastName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StreetAddress)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ZipCode)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BalanceDue)
            </td>
        </tr>
    }

</table>

